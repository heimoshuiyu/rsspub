{
  "tasks": {
    "dev": "deno run --watch -A main.ts",
    "test": "deno test --watch -A test.ts",
    "start": "deno run -A main.ts",
    "doc": "deno run --allow-write --allow-read doc.ts && deno doc .doc_gen.ts --json > .doc_gen.json && deno run -A doc-website.tsx"
  }
}
